{
  "private": true,
  "name": "isbn",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint . --max-warnings=0",
    "fmt": "prettier . --write",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "catalog:"
  },
  "devDependencies": {
    "eslint": "catalog:",
    "eslint-config": "workspace:*",
    "prettier": "catalog:",
    "typescript": "catalog:",
    "typescript-config": "workspace:*",
    "vitest": "catalog:"
  }
}
